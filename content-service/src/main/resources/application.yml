server:
  port: 8080
spring:
  application:
    # 服务名称
    name: content-service
  # 数据库
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.122.87.219:3306/content_center?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  # nacos注册中心
  cloud:
    nacos:
      discovery:
        server-addr: 47.122.87.219:8848
        #namespace: 7e1f4523-af8b-48ab-9ec2-368a30db92ee # nacos命名空间
        #group: DEFAULT_GROUP  # nacos命名空间id
        #cluster-name: NJ      # nacos集群名称

# mybatis 扫描配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.userservice.dto


stock:
  service:
    url: http://47.122.87.219:8100